namespace Library.Domain.Enums;

public enum UserRole
{
    /// <summary>Read-only store customer.</summary>
    Customer = 0,

    /// <summary>Can manage books and authors.</summary>
    Admin = 1
}
